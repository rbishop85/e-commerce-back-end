// task 1 - complete models

// * `Category`
//   * `id`
//     * Integer.
//     * Doesn't allow null values.
//     * Set as primary key.
//     * Uses auto increment.


// * `Product`
//   * `id`
//     * Integer.
//     * Doesn't allow null values.
//     * Set as primary key.
//     * Uses auto increment.
//   * `category_id`
//     * Integer.
//     * References the `Category` model's `id`.

// * `Tag`
//   * `id`
//     * Integer.
//     * Doesn't allow null values.
//     * Set as primary key.
//     * Uses auto increment.


// * `ProductTag`
//   * `id`
//     * Integer.
//     * Doesn't allow null values.
//     * Set as primary key.
//     * Uses auto increment.
//   * `product_id`
//     * Integer.
//     * References the `Product` model's `id`.
//   * `tag_id`
//     * Integer.
//     * References the `Tag` model's `id`.



// Task 2 - complete routes demoed in insomnia
    // convert routes to async/await

// Re-watch end of day-2 preclass (Missed it while talking to wife)

